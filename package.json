{
  "scripts": {
    "start": "node index.js",
    "start:dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\" \"cd image-server && npm start\"",
    "start:image-server": "cd image-server && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "setup": "concurrently \"cd backend && npm install\" \"cd frontend && npm install\"",
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
